{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "jandc-qms",
    "api-client-react",
    "api-zod",
    "db",
    "scripts",
    "mockup-sandbox"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@workspace/jandc-qms",
    "build": "npm run build --workspace=@workspace/jandc-qms",
    "typecheck": "tsc --build",
    "lint": "npm run typecheck"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
